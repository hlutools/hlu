(function(){
'use strict';
const C=window.HLU_CONFIG||{};const BASE=C.BASE_PATH||'/hlu/';const Exam=window.HLUExam;const Toolkit=window.HLUToolkit;
const $=s=>document.querySelector(s),$$=s=>Array.from(document.querySelectorAll(s));
const ROOT=$('#viewRoot'),HEADER=$('#appHeader');
const KEYS={data:'hlu_tools_data_2209265',saved:'hlu_tools_saved_2209265',downloads:'hlu_tools_downloads_2209265',snapshot:'hlu_tools_snapshot_2209265',notices:'hlu_tools_notices_2209265',unreadNews:'hlu_tools_unread_news_2209265',lastSync:'hlu_tools_last_sync_2209265',analyticsDevice:'hlu_tools_analytics_device'};
const LEGACY={data:['hlu_tools_data_150926_3','hlu_tools_data_130926'],saved:['hlu_tools_saved_150926_3','hlu_tools_saved_130926'],downloads:['hlu_tools_downloads_150926_3','hlu_tools_downloads_130926'],snapshot:['hlu_tools_content_snapshot_150926_3'],notices:['hlu_tools_content_notifications_150926_3','hlu_tools_notices_130926'],unreadNews:['hlu_tools_unread_news_item_ids_150926_3'],lastSync:['hlu_tools_last_sync_150926_3','hlu_tools_last_sync_130926']};
const LABELS={news:'TIN TỨC',soft:'SOFT',docs:'TÀI LIỆU',firmware:'FIRMWARE'};const SECTION_SUB={soft:'Ứng dụng & công cụ dành cho kỹ thuật',docs:'Tài liệu hướng dẫn và kỹ thuật',firmware:'Firmware thiết bị các hãng',news:'Tin tức & cập nhật VNPT'};
const ART={home:'home_header_mockup_210926.webp',search:'search_header_v508.webp',saved:'saved_header_v508.webp',downloads:'downloads_header_v508.webp',notifications:'notifications_header_v508.webp',resources:'resources_header_220926.webp',toolkit:'toolkit_header_mockup_210926.webp'};
let resources=[],route={name:'home',params:{}},currentItem=null,detailBack='home',toastTimer=0,loading=false,apiLatency=null,publicIpValue='--';
let examRuntime={bank:null,source:'',message:'',loading:false,attempt:null,selected:{},marked:{},revealed:{},index:0,submitted:false,timedOut:false,deadline:0,historySaved:false,timer:null,configCount:20,configCustom:false};

function read(k,f){try{const v=localStorage.getItem(k);return v==null?f:JSON.parse(v);}catch(_){return f;}}function write(k,v){try{localStorage.setItem(k,JSON.stringify(v));}catch(_){}}function remove(k){try{localStorage.removeItem(k);}catch(_){}}
function migrate(){for(const [target,sources] of Object.entries(LEGACY)){const key=KEYS[target];if(localStorage.getItem(key)!=null)continue;for(const old of sources){const raw=localStorage.getItem(old);if(raw!=null){localStorage.setItem(key,raw);break;}}}if(localStorage.getItem(KEYS.unreadNews)==null)write(KEYS.unreadNews,[]);if(localStorage.getItem(KEYS.notices)==null)write(KEYS.notices,[]);}
function t(v){return String(v==null?'':v)}function esc(v){return t(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));}function strip(v){const d=document.createElement('div');d.innerHTML=t(v);return(d.textContent||'').trim();}function fold(v){return t(v).normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();}
function first(o,names,f=''){for(const n of names){if(o&&o[n]!=null&&o[n]!=='')return o[n];}return f;}function bool(v){if(v==null||v==='')return true;if(typeof v==='boolean')return v;return!['false','0','no','off','ẩn','an','không','khong','inactive'].includes(fold(v).trim());}
function safeUrl(v){const raw=t(v).trim();if(!raw)return'';try{const u=new URL(raw,location.href);return['http:','https:'].includes(u.protocol)?u.href:'';}catch(_){return'';}}
function section(v){const s=fold(v).replace(/[_-]+/g,' ').trim();if(s.includes('news')||s.includes('tin'))return'news';if(s.includes('soft')||s.includes('phan mem'))return'soft';if(s.includes('firm')||s.includes('rom'))return'firmware';return'docs';}
function normalize(row,i,forced){const title=first(row,['title','Title','name','Name','ten','Tên'],'Không có tiêu đề');return{id:t(first(row,['id','ID','Id'],`item-${i}`)),section:section(forced||first(row,['section','Section','category','Category'],'docs')),title:t(title),brand:t(first(row,['brand','Brand','hang','Hãng'],'')),model:t(first(row,['model','Model'],'')),version:t(first(row,['version','Version'],'')),size:t(first(row,['size','Size'],'')),description:strip(first(row,['description','Description','moTa','MoTa'],'')),viewUrl:safeUrl(first(row,['viewUrl','viewURL','url','URL'],'')),downloadUrl:safeUrl(first(row,['downloadUrl','downloadURL','fileUrl','fileURL'],'')),resolvedViewUrl:safeUrl(first(row,['resolvedViewUrl','resolvedViewURL'],'')),resolvedDownloadUrl:safeUrl(first(row,['resolvedDownloadUrl','resolvedDownloadURL'],'')),fileType:t(first(row,['fileType','FileType'],'')),visible:bool(first(row,['visible','Visible'],true)),sortOrder:Number(first(row,['sortOrder','order'],0))||0,createdAt:t(first(row,['createdAt','created'],'')),updatedAt:t(first(row,['updatedAt','updated','date','Date'],'')),iconUrl:safeUrl(first(row,['iconUrl','iconURL','thumbnail','thumb'],'')),imageUrl:safeUrl(first(row,['imageUrl','imageURL','anh','image'],'')),note:strip(first(row,['note','Note','ghiChu'],''))};}
function extract(payload){if(Array.isArray(payload))return payload.map(normalize);if(!payload||typeof payload!=='object')return[];if(payload.success===false)throw new Error(payload.message||payload.error||'API_ERROR');const direct=first(payload,['data','items','result','rows','resources'],null);if(Array.isArray(direct))return direct.map(normalize);if(direct&&typeof direct==='object')return extract(direct);let out=[];['news','soft','docs','firmware'].forEach(k=>{if(Array.isArray(payload[k]))out=out.concat(payload[k].map((r,i)=>normalize(r,i,k)));});return out;}
function sortRows(rows){return rows.filter(x=>x.visible).sort((a,b)=>a.sortOrder-b.sortOrder||t(b.updatedAt).localeCompare(t(a.updatedAt),'vi')||a.title.localeCompare(b.title,'vi'));}
function saved(){const v=read(KEYS.saved,[]);return Array.isArray(v)?v.map(t):[];}function downloads(){const v=read(KEYS.downloads,[]);return Array.isArray(v)?v.map(x=>typeof x==='string'?x:t(x&&x.title)).filter(Boolean):[];}function notices(){const v=read(KEYS.notices,[]);return Array.isArray(v)?v:[];}function unreadNews(){const v=read(KEYS.unreadNews,[]);return Array.isArray(v)?v.map(t):[];}
function bestImage(x){return x.imageUrl||x.iconUrl||'';}function bestView(x){return x.resolvedViewUrl||x.viewUrl||'';}function downloadRequest(x){return x.resolvedDownloadUrl||x.downloadUrl||'';}function displayDate(x){return x.createdAt||x.updatedAt||'';}function fmtDate(v){if(!v)return'';const d=new Date(v);if(Number.isNaN(d.getTime()))return t(v);return new Intl.DateTimeFormat('vi-VN',{day:'2-digit',month:'2-digit',year:'numeric'}).format(d);}function dateMs(v){const n=Date.parse(v||'');return Number.isNaN(n)?0:n;}
function driveId(url){const m=t(url).match(/\/file\/d\/([^/?#]+)|[?&]id=([^&#]+)/i);return m?(m[1]||m[2]||''):'';}function previewUrl(x){const u=safeUrl(bestView(x)||x.downloadUrl);if(!u)return'';const id=driveId(u);return id?`https://drive.google.com/file/d/${encodeURIComponent(id)}/preview`:u;}
function empty(icon,title,desc){return`<div class="empty"><span>${icon}</span><b>${esc(title)}</b><p>${esc(desc||'')}</p></div>`;}function toast(msg){const el=$('#toast');el.textContent=msg;el.classList.add('show');clearTimeout(toastTimer);toastTimer=setTimeout(()=>el.classList.remove('show'),2400);}
function snapshot(x){return{id:x.id,section:x.section,version:x.version||'',updatedAt:x.updatedAt||''};}
function unreadNoticeCount(){return notices().filter(n=>n.unread!==false).length;}
function notifyMessage(item,type){if(type==='VERSION_UPDATE')return item.section==='news'?'Tin tức được cập nhật':`${LABELS[item.section]||'Nội dung'} được cập nhật`;return item.section==='news'?'Tin tức mới':item.section==='soft'?'Phần mềm mới':item.section==='docs'?'Tài liệu mới':'Firmware mới';}
function processUpdates(fresh){const old=read(KEYS.snapshot,[]);if(!Array.isArray(old)||!old.length){write(KEYS.snapshot,fresh.map(snapshot));return;}const map=new Map(old.map(x=>[t(x.id),x]));const un=new Set(unreadNews());const all=notices();const add=[];fresh.forEach((item,i)=>{const prev=map.get(item.id);let type='';if(!prev){type='NEW_ITEM';if(item.section==='news')un.add(item.id);}else if(item.section==='news'&&prev.updatedAt&&item.updatedAt&&prev.updatedAt!==item.updatedAt)type='VERSION_UPDATE';else if(item.section!=='news'&&item.version&&prev.version!==item.version)type='VERSION_UPDATE';if(type)add.push({notificationId:`${type}-${item.id}-${item.updatedAt||item.version||Date.now()+i}`,itemId:item.id,section:item.section,type,title:item.title,message:notifyMessage(item,type),createdAt:new Date().toISOString(),unread:true});});if(add.length){const m=new Map();[...add,...all].forEach(n=>{if(!m.has(n.notificationId))m.set(n.notificationId,n);});write(KEYS.notices,[...m.values()].slice(0,100));if('Notification'in window&&Notification.permission==='granted'){try{new Notification(`HLU TOOLS có ${add.length} nội dung mới/cập nhật`,{body:add[0].title,icon:BASE+'assets/icons/icon-192.png'});}catch(_){}}}const valid=new Set(fresh.filter(x=>x.section==='news').map(x=>x.id));write(KEYS.unreadNews,[...un].filter(id=>valid.has(id)));write(KEYS.snapshot,fresh.map(snapshot));}
async function loadData(force=false,silent=false){if(loading)return;loading=true;const url=C.API_URL;const start=performance.now();try{const ctrl=new AbortController(),tm=setTimeout(()=>ctrl.abort(),C.API_TIMEOUT||20000);const r=await fetch(url+(url.includes('?')?'&':'?')+'_='+Date.now(),{cache:'no-store',redirect:'follow',signal:ctrl.signal});clearTimeout(tm);if(!r.ok)throw new Error('HTTP '+r.status);const payload=await r.json();const fresh=sortRows(extract(payload));if(!fresh.length)throw new Error('EMPTY_DATA');apiLatency=Math.round(performance.now()-start);processUpdates(fresh);resources=fresh;write(KEYS.data,fresh);write(KEYS.lastSync,new Date().toISOString());updateChrome();if(force&&!silent)toast(`Đã đồng bộ ${fresh.length} nội dung`);if(route.name==='home')render();}
catch(e){const cache=read(KEYS.data,[]);if(Array.isArray(cache)&&cache.length)resources=sortRows(cache.map(normalize));if(!silent)toast('Không thể đồng bộ, đang dùng dữ liệu đã lưu');updateChrome();if(route.name==='home')render();track('SYNC_ERROR',{errorCode:e.name||'FETCH'});}finally{loading=false;}}
function clearSync(){remove(KEYS.data);return loadData(true,false);}

function deviceId(){let id=localStorage.getItem(KEYS.analyticsDevice);if(!id){id=crypto.randomUUID?crypto.randomUUID():`${Date.now()}-${Math.random()}`;localStorage.setItem(KEYS.analyticsDevice,id);}return id;}const sessionId=crypto.randomUUID?crypto.randomUUID():`${Date.now()}-${Math.random()}`;
function track(name,opt={}){const allowed=new Set(['APP_OPEN','SESSION_START','HOME_VIEW','SECTION_VIEW','CONTENT_VIEW','DOWNLOAD_CLICK','VIEW_CLICK','SAVE_ITEM','UNSAVE_ITEM','SEARCH','SEARCH_NO_RESULT','NEWS_READ','NOTIFICATION_OPEN','SYNC_START','SYNC_SUCCESS','SYNC_ERROR','FEEDBACK_OPEN','FEEDBACK_SENT','FEEDBACK_ERROR']);if(!allowed.has(name)||!C.API_URL)return;const ev={eventName:name,occurredAt:new Date().toISOString(),deviceId:deviceId(),sessionId,appVersion:C.APP_VERSION||'',section:t(opt.section).slice(0,30),contentId:t(opt.contentId).slice(0,120),contentTitle:t(opt.contentTitle).slice(0,300),searchQuery:t(opt.searchQuery).slice(0,300),errorCode:t(opt.errorCode).slice(0,80),metadata:{source:'web',androidVersion:'Web/PWA'}};if(Number.isInteger(opt.resultCount)&&opt.resultCount>=0)ev.resultCount=opt.resultCount;fetch(C.API_URL,{method:'POST',headers:{'Content-Type':'text/plain;charset=UTF-8'},body:JSON.stringify({action:'analytics',events:[ev]}),keepalive:true}).catch(()=>{});}

function openDrawer(){$('#drawer').classList.add('open');$('#scrim').classList.remove('hidden');$('#drawer').setAttribute('aria-hidden','false');}function closeDrawer(){$('#drawer').classList.remove('open');$('#scrim').classList.add('hidden');$('#drawer').setAttribute('aria-hidden','true');}
function parseRoute(){const q=new URLSearchParams(location.search);return{name:q.get('view')||'home',params:Object.fromEntries(q.entries())};}
function routeUrl(name,params={}){const q=new URLSearchParams();if(name!=='home')q.set('view',name);for(const[k,v]of Object.entries(params))if(k!=='view'&&v!=null&&v!=='')q.set(k,v);return BASE+(q.toString()?'?'+q:'');}
function go(name,params={},replace=false){route={name,params:{...params}};history[replace?'replaceState':'pushState']({route},'',routeUrl(name,params));closeDrawer();render();window.scrollTo(0,0);}
function back(){if(history.length>1)history.back();else go('home',{},true);}
function headerConfig(){const n=route.name;if(ART[n]){if(n==='home')return{art:ART[n],left:'menu',right:'bell'};if(n==='resources')return{art:ART[n],left:'back'};return{art:ART[n],left:'back-hidden'};}if(n==='section')return{title:LABELS[route.params.section]||'TÀI NGUYÊN',left:'back'};if(n==='detail')return{title:currentItem?LABELS[currentItem.section]:'CHI TIẾT',left:'back',right:currentItem?'fav':''};if(n==='viewer')return{title:currentItem?currentItem.title:'XEM NỘI DUNG',left:'back',right:'external'};if(n==='tool')return{title:(Toolkit.tools.find(x=>x.key===route.params.tool)||{}).title||'Network Toolkit',left:'back'};if(n.startsWith('exam'))return{title:n==='exam'?'E-LEARNING':n==='exam-config'?'CẤU HÌNH ĐỀ THI':examRuntime.submitted?'KẾT QUẢ THI':route.params.mode==='test'?'THI THỬ':'LUYỆN TẬP',left:'back',learning:true};if(n==='feedback')return{title:'GÓP Ý',left:'back'};if(n==='settings')return{title:'CÀI ĐẶT',left:'back'};if(n==='about')return{title:'Giới thiệu',left:'back'};if(n==='news')return{title:'TIN TỨC',left:'back'};return{title:'HLU TOOLS',left:'back'};}
function updateChrome(){const h=headerConfig(),art=$('#headerArtwork'),tech=$('#headerTech');art.classList.toggle('hidden',!h.art);tech.classList.toggle('hidden',!!h.art);HEADER.classList.remove('learning','section-soft','section-docs','section-firmware');HEADER.classList.toggle('learning',!!h.learning);if(route.name==='section'&&['soft','docs','firmware'].includes(route.params.section))HEADER.classList.add('section-'+route.params.section);if(h.art)$('#headerImage').src=BASE+'assets/android-v220926/'+h.art;else $('#headerTitle').textContent=h.title||'HLU TOOLS';const left=$('#headerLeft'),right=$('#headerRight');left.classList.toggle('hidden',!h.left);$('#headerLeftIcon').textContent=h.left==='menu'?'☰':h.left==='back-hidden'?'':'‹';right.classList.toggle('hidden',!h.right);$('#headerRightIcon').textContent=h.right==='bell'?'♢':h.right==='fav'?(currentItem&&saved().includes(currentItem.id)?'♥':'♡'):h.right==='external'?'↗':'';const nb=$('#headerBadge'),cnt=unreadNoticeCount();nb.textContent=cnt>99?'99+':cnt;nb.classList.toggle('hidden',h.right!=='bell'||cnt===0);const net=navigator.onLine;$('#drawerStatusDot').classList.toggle('online',net);$('#drawerStatusText').textContent=net?'Dịch vụ đang hoạt động':'Đang chờ kết nối';$('#drawerVersion').textContent='Version '+(C.APP_VERSION||'220926.5');$$('.bottom-nav button').forEach(b=>b.classList.toggle('active',b.dataset.route===(['home','search','toolkit','saved','downloads'].includes(route.name)?route.name:'home')));$$('#drawerNav button').forEach(b=>b.classList.toggle('active',b.dataset.route===route.name||(route.name==='section'&&b.dataset.section===route.params.section)));const dn=$('#drawerNewsBadge'),uc=unreadNews().length;dn.textContent=uc>99?'99+':uc;dn.classList.toggle('hidden',!uc);}
function card(item){const isNew=item.section==='news'&&unreadNews().includes(item.id),fav=saved().includes(item.id),img=bestImage(item),tag=isNew?'Mới':item.section==='soft'?(item.version||'SOFT'):item.section==='firmware'?(item.version||'Firmware'):(item.section==='docs'?(item.fileType||'Tài liệu'):(item.brand||'Tin tức'));return`<article class="content-card ${isNew?'new':''}" data-open-item="${esc(item.id)}"><div class="content-thumb">${img?`<img src="${esc(img)}" alt="" loading="lazy" referrerpolicy="no-referrer">`:(item.section==='news'?'NEWS':'▣')}</div><div class="content-copy"><span class="tag ${isNew?'new':''}">${esc(tag)}</span><h3>${esc(item.title)}</h3><p>${esc(item.description||item.note||item.brand||SECTION_SUB[item.section])}</p>${displayDate(item)?`<time>${esc(fmtDate(displayDate(item)))}</time>`:''}</div><button class="fav-btn ${fav?'saved':''}" data-fav="${esc(item.id)}" type="button">${fav?'♥':'♡'}</button></article>`;}
function filterItems(query,sec='',brand=''){const q=fold(query).trim();return resources.filter(x=>(!sec||x.section===sec)&&(!brand||fold(x.brand)===fold(brand))&&(!q||fold([x.title,x.brand,x.model,x.version,x.description,x.note,x.fileType].join(' ')).includes(q)));}
function sortedNews(){const un=new Set(unreadNews());return resources.filter(x=>x.section==='news').sort((a,b)=>Number(un.has(b.id))-Number(un.has(a.id))||dateMs(b.updatedAt)-dateMs(a.updatedAt)||a.sortOrder-b.sortOrder);}

function renderHome(){const news=sortedNews().slice(0,3),un=unreadNews().length;ROOT.innerHTML=`<div class="page no-pad"><section class="status-card"><div class="status-head"><strong>Tình trạng kết nối</strong><small id="networkUpdated">Cập nhật: --</small><button data-network-refresh>↻</button></div><div class="status-grid"><div class="status-cell ${navigator.onLine?'online':''}"><span>◎</span><b>${navigator.onLine?'Online':'Offline'}</b><small>API</small></div><div class="status-cell"><span>⌁</span><b>${esc((navigator.connection&&navigator.connection.effectiveType)||'Web')}</b><small>Kết nối</small></div><div class="status-cell"><span>◉</span><b id="homeIp">${esc(publicIpValue)}</b><small>Public IP</small></div><div class="status-cell"><span>◴</span><b>${apiLatency==null?'--':apiLatency+' ms'}</b><small>API RTT</small></div></div></section><div class="section-title"><span>Công cụ mạng</span><button data-route="toolkit">Xem tất cả ›</button></div><div class="home-tools">${Toolkit.tools.filter(x=>x.group==='quick').map(x=>`<button class="home-tool" data-tool="${x.key}"><span class="tool-icon">${x.icon}</span><small>${esc(x.title==='IP'?'IP':x.title==='Wi-Fi Info'?'Wifi Signal':x.title)}</small></button>`).join('')}</div><div class="section-title"><span>Tài nguyên</span><button data-route="resources">Xem tất cả ›</button></div><div class="resource-grid">${resourceCard('soft','SOFT','Ứng dụng & công cụ',resources.filter(x=>x.section==='soft').length+' ứng dụng','home_card_soft_bg_210926.webp')}${resourceCard('docs','TÀI LIỆU','Hướng dẫn kỹ thuật',resources.filter(x=>x.section==='docs').length+' tài liệu','home_card_docs_bg_210926.webp')}${resourceCard('firmware','FIRMWARE','Firmware thiết bị',resources.filter(x=>x.section==='firmware').length+' phiên bản','home_card_firmware_bg_210926.webp')}${resourceCard('exam','E-LEARNING','Ôn tập & kỳ thi','Học tập nội bộ','home_card_learning_bg_210926.webp')}</div><section class="home-news-wrap"><div class="section-title"><span>Tin tức ${un?`<em class="menu-badge">${un} mới</em>`:''}</span><button data-route="news">Xem tất cả ›</button></div><div class="news-list">${news.length?news.map(card).join(''):empty('📰','Chưa có tin tức','Dữ liệu đang được cập nhật.')}</div></section></div>`;track('HOME_VIEW');loadPublicIp(false);}
function resourceCard(kind,title,sub,count,img){const attr=kind==='exam'?'data-route="exam"':`data-section="${kind}"`;return`<button class="resource-card" ${attr}><img src="${BASE}assets/android-v220926/${img}" alt=""><span class="resource-copy"><b>${title}</b><span>${sub}</span><small>${count} ›</small></span></button>`;}
async function loadPublicIp(force){if(publicIpValue!=='--'&&!force)return;try{publicIpValue=await Toolkit.publicIp();if(route.name==='home'){$('#homeIp')&&($('#homeIp').textContent=publicIpValue);$('#networkUpdated')&&($('#networkUpdated').textContent='Cập nhật: '+new Date().toLocaleTimeString('vi-VN',{hour:'2-digit',minute:'2-digit'}));}}catch(_){publicIpValue='N/A';}}
function renderResources(){const recent=resources.filter(x=>['soft','docs','firmware'].includes(x.section)).sort((a,b)=>dateMs(b.updatedAt)-dateMs(a.updatedAt)||a.sortOrder-b.sortOrder).slice(0,6);ROOT.innerHTML=`<div class="page no-pad"><div class="resource-grid" style="padding-top:12px">${resourceCard('soft','SOFT','Ứng dụng & công cụ',resources.filter(x=>x.section==='soft').length+' ứng dụng','home_card_soft_bg_210926.webp')}${resourceCard('docs','TÀI LIỆU','Hướng dẫn kỹ thuật',resources.filter(x=>x.section==='docs').length+' tài liệu','home_card_docs_bg_210926.webp')}${resourceCard('firmware','FIRMWARE','Firmware thiết bị',resources.filter(x=>x.section==='firmware').length+' phiên bản','home_card_firmware_bg_210926.webp')}${resourceCard('exam','E-LEARNING','Ôn tập & kỳ thi','Học tập nội bộ','home_card_learning_bg_210926.webp')}</div><div class="section-title"><span>Mới cập nhật</span></div><div class="recent-list">${recent.length?recent.map(card).join(''):empty('▣','Chưa có tài nguyên cập nhật','')}</div></div>`;}
function renderSearch(){const q=route.params.q||'';const rows=filterItems(q);ROOT.innerHTML=`<div class="page"><label class="search-box"><span>⌕</span><input id="searchInput" value="${esc(q)}" placeholder="Tìm kiếm..."></label><div class="summary">${q?`${rows.length} kết quả cho “${esc(q)}”`:`Tất cả nội dung · ${rows.length} mục`}</div><div class="content-list" style="padding:0">${rows.length?rows.map(card).join(''):empty('⌕','Không tìm thấy kết quả','Thử một từ khóa khác.')}</div></div>`;}
function renderSection(){const sec=route.params.section||'soft',q=route.params.q||'',brand=route.params.brand||'';const brands=['docs','firmware'].includes(sec)?[...new Set(resources.filter(x=>x.section===sec&&x.brand).map(x=>x.brand))].sort((a,b)=>a.localeCompare(b,'vi')):[];const rows=filterItems(q,sec,brand);ROOT.innerHTML=`<div class="page"><label class="search-box"><span>⌕</span><input id="sectionSearch" value="${esc(q)}" placeholder="Tìm ${LABELS[sec]||'nội dung'}..."></label>${brands.length?`<div class="chip-row"><button class="chip ${!brand?'active':''}" data-brand="">Tất cả</button>${brands.map(x=>`<button class="chip ${brand===x?'active':''}" data-brand="${esc(x)}">${esc(x)}</button>`).join('')}</div>`:''}<div class="summary">${rows.length} mục • ${esc(SECTION_SUB[sec])}</div><div class="content-list" style="padding:0">${rows.length?rows.map(card).join(''):empty('▣','Chưa có nội dung','Dữ liệu đang được cập nhật.')}</div></div>`;track('SECTION_VIEW',{section:sec});}
function renderNews(){const rows=sortedNews(),n=unreadNews().length;ROOT.innerHTML=`<div class="page no-pad">${n?`<div class="banner">${n} bài viết mới chưa đọc</div>`:''}<div class="news-list">${rows.length?rows.map(card).join(''):empty('📰','Chưa có tin tức','')}</div></div>`;}
function renderSaved(){const ids=saved(),rows=ids.map(id=>resources.find(x=>x.id===id)).filter(Boolean);ROOT.innerHTML=`<div class="page"><div class="summary">${rows.length} nội dung đã lưu</div><div class="content-list" style="padding:0">${rows.length?rows.map(card).join(''):empty('♡','Chưa có nội dung đã lưu','Nhấn biểu tượng trái tim để lưu nội dung.')}</div></div>`;}
function renderDownloads(){const rows=downloads();ROOT.innerHTML=`<div class="page no-pad"><div class="download-toolbar"><span>${rows.length} mục đã tải</span>${rows.length?'<button data-clear-downloads>XÓA</button>':''}</div><div class="download-list">${rows.length?rows.map(x=>`<article class="download-row" data-download-title="${esc(x)}"><span>⇩</span><b>${esc(x)}</b></article>`).join(''):empty('⇩','Chưa có lịch sử Download','Các nội dung đã tải sẽ xuất hiện tại đây.')}</div></div>`;}
function renderNotifications(){const rows=notices(),un=rows.filter(x=>x.unread!==false).length;ROOT.innerHTML=`<div class="page no-pad"><div class="notice-toolbar"><b>THÔNG BÁO</b>${un?'<button data-mark-all>ĐỌC HẾT</button>':''}</div><div class="notice-list">${rows.length?rows.map(n=>`<article class="notice-card ${n.unread!==false?'unread':''}" data-notice="${esc(n.notificationId)}" data-notice-item="${esc(n.itemId)}"><span class="emoji">${n.section==='news'?'📰':n.section==='soft'?'🧰':n.section==='docs'?'📄':'⚙️'}</span><h3>${esc(n.title)}</h3><p>${esc(n.message)}</p><time>${esc(fmtDate(n.createdAt))}</time></article>`).join(''):empty('♢','Chưa có thông báo','Nội dung mới hoặc cập nhật sẽ hiển thị tại đây.')}</div></div>`;}
function renderDetail(){if(!currentItem){ROOT.innerHTML=empty('▣','Không tìm thấy nội dung','');return;}const x=currentItem,img=bestImage(x),meta=[['Phiên bản',x.version],['Dung lượng',x.size],['Định dạng',x.fileType],['Hãng',x.brand],['Model',x.model]].filter(v=>v[1]);ROOT.innerHTML=`<article class="detail"><h2>${esc(x.title)}</h2>${displayDate(x)?`<time>${esc(fmtDate(displayDate(x)))}</time>`:''}${x.description?`<div class="detail-text">${esc(x.description)}</div>`:''}${x.note?`<div class="detail-note">${esc(x.note)}</div>`:''}${img?`<img src="${esc(img)}" alt="" referrerpolicy="no-referrer">`:''}${meta.length?`<div class="meta">${meta.map(v=>`<div><b>${v[0]}:</b> ${esc(v[1])}</div>`).join('')}</div>`:''}<div class="actions">${downloadRequest(x)?'<button class="primary" data-download-current>⇩ TẢI XUỐNG</button>':''}${previewUrl(x)?`<button class="secondary" data-view-current>↗ ${x.section==='news'?'XEM TIN':'XEM TÀI LIỆU'}</button>`:''}</div></article>`;}
function renderViewer(){const url=currentItem&&previewUrl(currentItem);ROOT.innerHTML=url?`<div class="viewer"><iframe src="${esc(url)}" title="${esc(currentItem.title)}"></iframe></div>`:empty('▣','Không thể mở nội dung','Liên kết xem không hợp lệ.');}
function renderSettings(){const ct=C.CONTACT||{};ROOT.innerHTML=`<div class="settings">${settingsSection('DỮ LIỆU',`<button class="settings-row" data-sync><span class="settings-icon">↻</span><span class="settings-copy"><b>Đồng bộ dữ liệu</b><small>Cập nhật dữ liệu mới nhất từ máy chủ</small></span><span>›</span></button><button class="settings-row" data-notify-enable><span class="settings-icon">♢</span><span class="settings-copy"><b>Thông báo Web</b><small>${'Notification'in window?(Notification.permission==='granted'?'Đã cấp quyền':'Nhấn để cấp quyền trình duyệt'):'Trình duyệt không hỗ trợ Web Notification'}</small></span><span>›</span></button>`)}${settingsSection('HỖ TRỢ',`<button class="settings-row" data-route="feedback"><span class="settings-icon">✉</span><span class="settings-copy"><b>Góp ý cho nhà phát triển</b><small>Báo lỗi, đề xuất tính năng hoặc góp ý khác</small></span><span>›</span></button>`)}${settingsSection('LIÊN HỆ',`<a class="settings-row" href="${esc(ct.facebook||'#')}" target="_blank" rel="noopener"><span class="settings-icon">f</span><span class="settings-copy"><b>Facebook</b><small>${esc(ct.developer||'Cường VNPT')}</small></span></a><a class="settings-row" href="${esc(ct.zalo||'#')}" target="_blank" rel="noopener"><span class="settings-icon">Z</span><span class="settings-copy"><b>Zalo</b><small>${esc(ct.developer||'Cường VNPT')}</small></span></a><a class="settings-row" href="tel:${esc(ct.phone||'')}"><span class="settings-icon">☎</span><span class="settings-copy"><b>Điện thoại</b><small>${esc(ct.phone||'')}</small></span></a>`)}${settingsSection('THÔNG TIN',`<div class="settings-row"><span class="settings-icon">⌂</span><span class="settings-copy"><b>Đơn vị</b><small>${esc(C.UNIT_NAME||'VNPT HOA LƯ')}</small></span></div><div class="settings-row"><span class="settings-icon">ⓘ</span><span class="settings-copy"><b>Thông tin phiên bản</b><small>HLU TOOLS v${esc(C.APP_VERSION||'220926.5')} • Web/PWA</small></span></div>`)}<div class="thanks">“Cảm ơn bạn đã đồng hành cùng HLU TOOLS”<br>Kết nối - Chia sẻ - Hiệu Quả</div></div>`;}
function settingsSection(title,body){return`<section class="settings-section"><div class="settings-title">${title}</div><div class="settings-card">${body}</div></section>`;}
function renderFeedback(){ROOT.innerHTML=`<form id="feedbackForm" class="feedback-form"><label>Loại góp ý<select id="feedbackType"><option>Báo lỗi</option><option>Đề xuất tính năng</option><option>Link tải lỗi</option><option>Nội dung sai</option><option>Khác</option></select></label><label>Tiêu đề<input id="feedbackTitle" maxlength="120" required></label><label>Nội dung<textarea id="feedbackContent" maxlength="1500" rows="7" required></textarea></label><label>Thông tin liên hệ (không bắt buộc)<input id="feedbackContact" maxlength="100"></label><button class="btn primary" type="submit">GỬI GÓP Ý</button></form>`;track('FEEDBACK_OPEN');}
function renderAbout(){ROOT.innerHTML=`<article class="about-card"><h2>HLU TOOLS</h2><p style="color:var(--muted)">Kết nối - Chia sẻ - Hiệu Quả</p><p>HLU TOOLS hỗ trợ tập trung và tra cứu nhanh các tài nguyên phục vụ công việc kỹ thuật như phần mềm, tài liệu, firmware, tin tức, Network Toolkit và E‑Learning. Bản Web/PWA đồng bộ cấu trúc và logic nghiệp vụ từ Android trong giới hạn an toàn của trình duyệt.</p><p>Mục tiêu là giúp người dùng tiết kiệm thời gian tìm kiếm, giảm phụ thuộc vào nhiều nguồn rời rạc và tiếp cận thông tin kỹ thuật nhanh hơn trên nhiều thiết bị.</p><p>Phiên bản Web: <b>${esc(C.APP_VERSION||'220926.5')}</b></p><p><a href="${esc(C.WEB_URL||location.origin+BASE)}" target="_blank" rel="noopener">${esc(C.WEB_URL||location.href)}</a></p><p style="text-align:center;color:var(--deep);font-weight:800">HLU TOOLS – Kết nối • Chia sẻ • Hiệu Quả</p></article>`;}
function renderToolkit(){const q=Toolkit.tools.filter(x=>x.group==='quick'),a=Toolkit.tools.filter(x=>x.group==='advanced');ROOT.innerHTML=`<div class="toolkit-groups"><div class="group-label">KIỂM TRA NHANH</div><div class="toolkit-grid">${q.map(toolCard).join('')}</div><div class="group-label">CÔNG CỤ CHUYÊN SÂU</div><div class="toolkit-grid">${a.map(toolCard).join('')}</div></div>`;}
function toolCard(x){return`<button class="tool-card" data-tool="${x.key}"><span class="tool-icon">${x.icon}</span><b>${esc(x.title)}</b><small>${esc(x.subtitle)}</small></button>`;}
function renderTool(){const key=route.params.tool,tool=Toolkit.tools.find(x=>x.key===key);if(!tool){ROOT.innerHTML=empty('⚒','Không tìm thấy công cụ','');return;}if(tool.native){ROOT.innerHTML=`<div class="tool-form"><div class="platform-card"><h3>${tool.icon} ${esc(tool.title)}</h3><p>${esc(Toolkit.nativeMessage(tool))}</p><p><strong>Lý do:</strong> trình duyệt không cho Web App quét Wi‑Fi/LAN, gửi ICMP, traceroute hoặc mở TCP socket tùy ý. Web không giả lập kết quả để tránh sai số.</p></div></div>`;return;}if(key==='subnet'){ROOT.innerHTML=`<div class="tool-form"><div class="field"><label>IPv4 / CIDR</label><input id="subnetInput" value="192.168.1.10/24"></div><button class="btn primary" data-subnet> TÍNH SUBNET </button><div id="toolResult"></div></div>`;return;}if(key==='mac-vendor'){ROOT.innerHTML=`<div class="tool-form"><div class="field"><label>Địa chỉ MAC</label><input id="macInput" placeholder="00:11:22:33:44:55"></div><button class="btn primary" data-mac>TRA CỨU</button><div id="toolResult"></div></div>`;return;}if(key==='ip'){ROOT.innerHTML=`<div class="tool-form"><button class="btn primary" data-ip>ĐỌC PUBLIC IP</button><div id="toolResult">${platformNote('Trên Web chỉ đọc được Public IP qua dịch vụ HTTPS. IPv4 LAN, Gateway, DNS và interface nội bộ bị trình duyệt giới hạn.')}</div></div>`;return;}if(key==='speed-test'){ROOT.innerHTML=`<div class="tool-form"><button class="btn primary" data-speed>BẮT ĐẦU SPEED TEST</button><div id="toolResult">${platformNote('Web sẽ thử LibreSpeed bằng fetch/CORS. Nếu máy chủ không cho CORS, công cụ sẽ báo rõ thay vì trả kết quả giả.')}</div></div>`;}}
function platformNote(x){return`<div class="platform-card">${esc(x)}</div>`;}function resultRows(title,rows){return`<div class="result-card"><h3>${esc(title)}</h3>${rows.map(x=>`<div class="result-row"><span>${esc(x[0])}</span><b>${esc(x[1])}</b></div>`).join('')}</div>`;}

async function ensureExam(refresh=true){if(examRuntime.bank)return examRuntime.bank;if(examRuntime.loading)return null;examRuntime.loading=true;renderExamHub();try{examRuntime.bank=await Exam.ExamRepository.load();examRuntime.source=Exam.ExamRepository.source;renderExamHub();if(refresh){const r=await Exam.ExamRepository.refreshOnline();examRuntime.bank=r.bank;examRuntime.source=r.source;examRuntime.message=r.userMessage||'';renderExamHub();}}finally{examRuntime.loading=false;}return examRuntime.bank;}
function renderExamHub(){const bank=examRuntime.bank,history=Exam.ExamHistoryStore.load();if(!bank){ROOT.innerHTML=`<div class="exam-wrap">${empty('♟','Đang tải E-Learning','')}</div>`;setTimeout(()=>ensureExam(true),0);return;}const topics=bank.topics||[],best=history.reduce((m,x)=>Math.max(m,Number(x.percent)||0),-1);ROOT.innerHTML=`${examRuntime.message?`<div class="exam-sync">${esc(examRuntime.message)}</div>`:''}<div class="exam-wrap"><section class="exam-overview"><h2>HLU TOOLS E-Learning</h2><p>${topics.length} chủ đề • ${bank.totalQuestionCount} câu hỏi</p><div class="exam-metrics"><div class="exam-metric"><b>${history.length}</b><small>Lượt đã làm</small></div><div class="exam-metric"><b>${best<0?'--':best+'%'}</b><small>Điểm cao nhất</small></div><div class="exam-metric"><b>${bank.meta.bankVersion?'v'+esc(bank.meta.bankVersion):'Local'}</b><small>Ngân hàng</small></div></div>${bank.meta.updatedAt?`<p style="font-size:11px;color:var(--muted2)">Cập nhật ngân hàng: ${esc(bank.meta.updatedAt)}</p>`:''}</section><div class="exam-title"><h3>LUYỆN TẬP THEO CHỦ ĐỀ</h3><p>Chọn một chủ đề để ôn tập và xem đáp án ngay sau khi trả lời.</p></div>${topics.length?`<div class="practice-scroll">${topics.map(tp=>`<article class="exam-card practice-card"><h4>${esc(tp.title)}</h4><p>${esc(tp.subtitle)}</p><small>${Exam.ExamRepository.questionsFor(tp.id).length} câu hiện có</small><button class="btn primary" data-practice="${esc(tp.id)}" ${Exam.ExamRepository.questionsFor(tp.id).length?'':'disabled'}>▶ Luyện tập</button></article>`).join('')}</div>`:empty('♟','Chưa có ngân hàng câu hỏi','')}<div class="exam-title"><h3>THI THỬ</h3><p>Làm đề theo thời gian và số câu đã cấu hình cho từng chủ đề.</p></div>${topics.length?`<section class="exam-card">${topics.map(tp=>`<button class="mock-row" data-exam-config="${esc(tp.id)}" style="width:100%;border:0;background:transparent;text-align:left"><span class="round">◴</span><div><b>${esc(tp.title)}</b><small>${Math.min(tp.examQuestionCount,Exam.ExamRepository.questionsFor(tp.id).length)} câu • ${tp.durationMinutes} phút</small></div><span>›</span></button>`).join('')}</section>`:''}<div class="exam-title"><h3>LỊCH SỬ KẾT QUẢ</h3><p>${history.length?'Hiển thị 5 kết quả gần nhất trên thiết bị này.':'Chưa có lượt luyện tập hoặc thi thử nào.'}</p></div>${history.length?history.slice(0,5).map(historyCard).join(''):platformNote('Kết quả sau khi hoàn thành bài sẽ được lưu tại đây.')}</div>`;}
function historyCard(x){let date='';try{date=new Intl.DateTimeFormat('vi-VN',{day:'2-digit',month:'2-digit',year:'numeric',hour:'2-digit',minute:'2-digit'}).format(new Date(x.finishedAt));}catch(_){}return`<article class="history-row"><span class="history-score">${esc(x.percent)}%</span><div><b>${esc(x.topicTitle)}</b><small>${x.mode==='test'?'Thi thử':'Luyện tập'} • ${esc(x.correct)}/${esc(x.total)} câu</small></div><small>${esc(date)}</small></article>`;}
async function renderExamConfig(){const bank=examRuntime.bank||await ensureExam(false);if(!bank)return;const tp=bank.topics.find(x=>x.id===route.params.topic);if(!tp){ROOT.innerHTML=empty('♟','Không tìm thấy chủ đề thi','');return;}const current=Exam.MOCK_COUNTS.includes(tp.examQuestionCount)?tp.examQuestionCount:Exam.DEFAULT_MOCK_COUNT;examRuntime.configCount=Exam.MOCK_COUNTS.includes(examRuntime.configCount)?examRuntime.configCount:current;ROOT.innerHTML=`<div class="exam-wrap"><section class="exam-card"><h2 style="margin:0;font-size:18px">${esc(tp.title)}</h2><p style="color:var(--muted)">${Exam.ExamRepository.questionsFor(tp.id).length} câu hiện có • ${tp.durationMinutes} phút</p></section><section class="exam-card"><label style="display:flex;align-items:center;gap:10px"><input id="customExam" type="checkbox" ${examRuntime.configCustom?'checked':''}><span><b>Tùy chỉnh đề thi</b><small style="display:block;color:var(--muted)">${examRuntime.configCustom?'Chọn số lượng câu muốn làm':`Dùng mặc định hiện tại: ${tp.examQuestionCount} câu`}</small></span></label>${examRuntime.configCustom?`<div class="exam-config-choice">${Exam.MOCK_COUNTS.map(n=>`<button class="chip ${examRuntime.configCount===n?'active':''}" data-exam-count="${n}">${n} câu</button>`).join('')}</div>`:''}</section><section class="exam-card"><b>Thời gian thi</b><p style="color:var(--muted)">${tp.durationMinutes} phút • cấu hình riêng theo chủ đề</p></section><button class="btn primary" data-start-mock="${esc(tp.id)}">▶ BẮT ĐẦU THI</button></div>`;}
async function startAttempt(topicId,mode,count){const bank=examRuntime.bank||await ensureExam(false);const tp=bank&&bank.topics.find(x=>x.id===topicId);if(!tp)return toast('Không tìm thấy chủ đề');const attempt=Exam.ExamEngine.createAttempt(bank,tp,mode,count);if(!attempt.questions.length)return toast('Chưa có câu hỏi cho chủ đề này');examRuntime={...examRuntime,attempt,selected:{},marked:{},revealed:{},index:0,submitted:false,timedOut:false,deadline:mode==='test'?Date.now()+tp.durationMinutes*60000:0,historySaved:false};go('exam-run',{topic:topicId,mode,count:count||''});}
function ensureAttemptFromRoute(){if(examRuntime.attempt&&examRuntime.attempt.topic.id===route.params.topic&&examRuntime.attempt.mode===route.params.mode)return true;const bank=examRuntime.bank;if(!bank)return false;const tp=bank.topics.find(x=>x.id===route.params.topic);if(!tp)return false;const count=Number(route.params.count)||undefined;examRuntime.attempt=Exam.ExamEngine.createAttempt(bank,tp,route.params.mode||'practice',count);examRuntime.selected={};examRuntime.marked={};examRuntime.revealed={};examRuntime.index=0;examRuntime.submitted=false;examRuntime.timedOut=false;examRuntime.deadline=(route.params.mode==='test')?Date.now()+tp.durationMinutes*60000:0;examRuntime.historySaved=false;return true;}
async function renderExamRun(){if(!examRuntime.bank){await ensureExam(false);}if(!ensureAttemptFromRoute()){ROOT.innerHTML=empty('♟','Không tìm thấy câu hỏi cho chủ đề này','');return;}const at=examRuntime.attempt;if(examRuntime.submitted){clearExamTimer();const score=Exam.ExamEngine.score(at,examRuntime.selected);if(!examRuntime.historySaved){Exam.ExamHistoryStore.add(at,score);examRuntime.historySaved=true;}ROOT.innerHTML=`<div class="exam-wrap">${examRuntime.timedOut?'<div class="exam-sync">Hết thời gian. Bài thi đã được tự động nộp và khóa thao tác.</div>':''}<section class="exam-card result-score"><div class="trophy">🏆</div><h2>${esc(at.topic.title)}</h2><strong>${score.percent}%</strong><p>Đúng ${score.correct}/${score.total} câu</p><button class="btn primary" data-route="exam">VỀ DANH SÁCH CHỦ ĐỀ</button></section><div class="exam-title"><h3>XEM LẠI ĐÁP ÁN</h3></div>${at.questions.map(q=>reviewQuestion(q,examRuntime.selected[q.source.id]||[])).join('')}</div>`;return;}const q=at.questions[examRuntime.index],sel=new Set(examRuntime.selected[q.source.id]||[]),revealed=at.mode==='practice'&&examRuntime.revealed[q.source.id]===true,correct=Exam.ExamEngine.isAnswerCorrect(q.source,[...sel]),remaining=at.mode==='test'?Math.max(0,Math.ceil((examRuntime.deadline-Date.now())/1000)):0;ROOT.innerHTML=`<div class="exam-wrap"><section class="exam-card"><h3 style="margin:0;color:#015acb">${esc(at.topic.title)}</h3><div class="exam-metrics"><div class="exam-metric"><b>${at.questions.length} câu</b><small>Tổng</small></div><div class="exam-metric"><b id="examTimer">${at.mode==='test'?formatTime(remaining):'Luyện tập'}</b><small>${at.mode==='test'?'Thời gian':'Chế độ'}</small></div><div class="exam-metric"><b>Xáo trộn</b><small>Đề</small></div></div></section><section class="exam-card"><div class="exam-run-head"><b>Câu ${examRuntime.index+1}/${at.questions.length}</b><span class="progress"><i style="width:${((examRuntime.index+1)/at.questions.length*100).toFixed(1)}%"></i></span></div><div class="question-text">${esc(q.source.text)}</div><div class="question-help">${q.source.type==='true_false'?'Chọn Đúng hoặc Sai.':'Có thể chọn một hoặc nhiều đáp án.'}</div><div class="option-list">${q.options.map((o,i)=>optionHtml(q,o,i,sel,revealed)).join('')}</div>${at.mode==='practice'&&q.source.type==='multi_choice'&&!revealed?`<button class="btn primary" data-reveal ${sel.size?'':'disabled'}>✓ KIỂM TRA ĐÁP ÁN</button>`:''}${revealed?`<div class="exam-feedback ${correct?'ok':''}"><b>${correct?'Chính xác':'Chưa chính xác'}</b>${q.source.explanation?`<p>${esc(q.source.explanation)}</p>`:''}</div>`:''}</section><div class="question-nav">${at.questions.map((qq,i)=>`<button class="${i===examRuntime.index?'active':(examRuntime.selected[qq.source.id]||[]).length?'answered':examRuntime.marked[qq.source.id]?'marked':''}" data-q-index="${i}">${i+1}</button>`).join('')}</div><div class="exam-actions"><button class="btn secondary" data-mark>☆ Đánh dấu</button><button class="btn secondary" data-prev ${examRuntime.index===0?'disabled':''}>‹ Trước</button><button class="btn primary" data-next ${(at.mode==='practice'&&!revealed)?'disabled':''}>${examRuntime.index<at.questions.length-1?'Câu tiếp':'Nộp'} ›</button></div>${at.mode==='test'&&examRuntime.index<at.questions.length-1?'<button class="btn primary" data-submit-exam>NỘP BÀI</button>':''}</div>`;if(at.mode==='test')startExamTimer();}
function optionHtml(q,o,i,sel,revealed){const selected=sel.has(o.id);let cls=selected?'selected ':'';if(revealed){if(o.correct)cls+='correct ';else if(selected)cls+='wrong ';}const key=q.source.type==='true_false'?(o.id==='true'?'Đ':'S'):String.fromCharCode(65+i);return`<button class="option ${cls}" data-option="${esc(o.id)}"><span class="option-key">${key}</span><span>${esc(o.text)}</span>${revealed&&o.correct?'<b style="margin-left:auto;color:#1e9e57">✓</b>':''}</button>`;}
function reviewQuestion(q,ids){const chosen=q.source.options.filter(o=>ids.includes(o.id)).map(o=>o.text).join('; ')||'Chưa trả lời',correct=q.source.options.filter(o=>o.correct).map(o=>o.text).join('; '),ok=Exam.ExamEngine.isAnswerCorrect(q.source,ids);return`<article class="review-card"><h4>${ok?'✅':'❌'} ${esc(q.source.text)}</h4><p>Bạn chọn: ${esc(chosen)}</p><p class="correct-text">Đáp án đúng: ${esc(correct)}</p>${q.source.explanation?`<p>${esc(q.source.explanation)}</p>`:''}</article>`;}
function formatTime(s){s=Math.max(0,s|0);return`${String(Math.floor(s/60)).padStart(2,'0')}:${String(s%60).padStart(2,'0')}`;}function clearExamTimer(){if(examRuntime.timer){clearInterval(examRuntime.timer);examRuntime.timer=null;}}function startExamTimer(){clearExamTimer();examRuntime.timer=setInterval(()=>{if(route.name!=='exam-run'||examRuntime.submitted){clearExamTimer();return;}const left=Math.max(0,Math.ceil((examRuntime.deadline-Date.now())/1000)),el=$('#examTimer');if(el)el.textContent=formatTime(left);if(left<=0){examRuntime.timedOut=true;examRuntime.submitted=true;clearExamTimer();render();}},500);}

function render(){clearExamTimer();currentItem=(route.params.id&&resources.find(x=>x.id===route.params.id))||currentItem;updateChrome();switch(route.name){case'home':renderHome();break;case'resources':renderResources();break;case'search':renderSearch();break;case'section':renderSection();break;case'news':renderNews();break;case'saved':renderSaved();break;case'downloads':renderDownloads();break;case'notifications':renderNotifications();break;case'detail':renderDetail();break;case'viewer':renderViewer();break;case'settings':renderSettings();break;case'feedback':renderFeedback();break;case'about':renderAbout();break;case'toolkit':renderToolkit();break;case'tool':renderTool();break;case'exam':renderExamHub();if(!examRuntime.bank)setTimeout(()=>ensureExam(true),0);break;case'exam-config':renderExamConfig();break;case'exam-run':renderExamRun();break;default:go('home',{},true);}}
function toggleFav(id){let a=saved();const had=a.includes(id);a=had?a.filter(x=>x!==id):[id,...a];write(KEYS.saved,a);toast(had?'Đã bỏ lưu':'Đã lưu nội dung');const item=resources.find(x=>x.id===id);if(item)track(had?'UNSAVE_ITEM':'SAVE_ITEM',{section:item.section,contentId:item.id,contentTitle:item.title});render();}
function markNewsRead(id){write(KEYS.unreadNews,unreadNews().filter(x=>x!==id));}function markItemNotices(id){write(KEYS.notices,notices().map(n=>n.itemId===id?{...n,unread:false}:n));}function openItem(id,from){const item=resources.find(x=>x.id===id);if(!item)return;currentItem=item;detailBack=from||route.name;if(item.section==='news'){markNewsRead(id);track('NEWS_READ',{section:'news',contentId:id,contentTitle:item.title});}markItemNotices(id);track('CONTENT_VIEW',{section:item.section,contentId:id,contentTitle:item.title});go('detail',{id});}
function recordDownload(item){write(KEYS.downloads,[item.title,...downloads().filter(x=>x!==item.title)].slice(0,100));track('DOWNLOAD_CLICK',{section:item.section,contentId:item.id,contentTitle:item.title});}
function doDownload(){if(!currentItem)return;const u=safeUrl(downloadRequest(currentItem)||bestView(currentItem));if(!u)return toast('Nội dung chưa có liên kết tải');recordDownload(currentItem);window.open(u,'_blank','noopener');toast('Đã lưu vào lịch sử Download');}
async function feedbackSubmit(e){e.preventDefault();const p={action:'feedback',type:$('#feedbackType').value,title:$('#feedbackTitle').value.trim(),content:$('#feedbackContent').value.trim(),contact:$('#feedbackContact').value.trim(),appVersion:C.APP_VERSION||'220926.5',device:navigator.userAgent,androidVersion:'Web/PWA'};if(!p.title||!p.content)return toast('Vui lòng nhập tiêu đề và nội dung');try{const r=await fetch(C.API_URL,{method:'POST',headers:{'Content-Type':'text/plain;charset=UTF-8'},body:JSON.stringify(p),redirect:'follow'});const j=await r.json();if(j.success!==true)throw new Error();toast('Đã gửi góp ý. Cảm ơn bạn!');track('FEEDBACK_SENT');go('settings',{},true);}catch(_){toast('Không thể gửi góp ý. Vui lòng thử lại.');track('FEEDBACK_ERROR');}}
async function enableNotifications(){if(!('Notification'in window))return toast('Trình duyệt không hỗ trợ Web Notification');try{const p=await Notification.requestPermission();toast(p==='granted'?'Đã bật thông báo Web':'Chưa cấp quyền thông báo');renderSettings();}catch(_){toast('Không thể yêu cầu quyền thông báo');}}

async function handleClick(e){const el=e.target.closest('button,a,[data-open-item]');if(!el)return;if(el.dataset.route){e.preventDefault();go(el.dataset.route);return;}if(el.dataset.section){openSection(el.dataset.section);return;}if(el.dataset.tool){go('tool',{tool:el.dataset.tool});return;}if(el.dataset.openItem){openItem(el.dataset.openItem,route.name);return;}if(el.dataset.fav){e.stopPropagation();toggleFav(el.dataset.fav);return;}if(el.hasAttribute('data-network-refresh')){apiLatency=null;publicIpValue='--';loadData(true,true);loadPublicIp(true);return;}if(el.hasAttribute('data-mark-all')){write(KEYS.notices,notices().map(n=>({...n,unread:false})));render();return;}if(el.dataset.notice){write(KEYS.notices,notices().map(n=>n.notificationId===el.dataset.notice?{...n,unread:false}:n));track('NOTIFICATION_OPEN');openItem(el.dataset.noticeItem,'notifications');return;}if(el.dataset.downloadTitle){const item=resources.find(x=>x.title===el.dataset.downloadTitle);if(item)openItem(item.id,'downloads');return;}if(el.hasAttribute('data-clear-downloads')){if(confirm('Xóa toàn bộ lịch sử Download?')){write(KEYS.downloads,[]);render();}return;}if(el.hasAttribute('data-download-current')){doDownload();return;}if(el.hasAttribute('data-view-current')){if(currentItem){track('VIEW_CLICK',{section:currentItem.section,contentId:currentItem.id,contentTitle:currentItem.title});go('viewer',{id:currentItem.id});}return;}if(el.hasAttribute('data-sync')){track('SYNC_START');clearSync().then(()=>track('SYNC_SUCCESS',{resultCount:resources.length}));return;}if(el.hasAttribute('data-notify-enable')){enableNotifications();return;}if(el.dataset.brand!=null){go('section',{section:route.params.section,brand:el.dataset.brand,q:route.params.q||''},true);return;}if(el.hasAttribute('data-subnet')){try{const x=Toolkit.subnet($('#subnetInput').value);$('#toolResult').innerHTML=resultRows('Kết quả',Object.entries({IP:x.ip,CIDR:'/'+x.prefix,'Subnet mask':x.mask,Network:x.network,Broadcast:x.broadcast,'First host':x.firstHost,'Last host':x.lastHost,'Số host':x.hostCount}));}catch(err){$('#toolResult').innerHTML=platformNote(err.message);}return;}if(el.hasAttribute('data-mac')){try{$('#toolResult').innerHTML=resultRows('MAC Vendor',[['Kết quả',await Toolkit.macVendor($('#macInput').value)]]);}catch(err){$('#toolResult').innerHTML=platformNote(err.message);}return;}if(el.hasAttribute('data-ip')){try{$('#toolResult').innerHTML=resultRows('IP',[['Public IP',await Toolkit.publicIp()],['Nền tảng','Web/PWA']]);}catch(err){$('#toolResult').innerHTML=platformNote('Không đọc được Public IP: '+err.message);}return;}if(el.hasAttribute('data-speed')){const btn=el;btn.disabled=true;btn.textContent='ĐANG KIỂM TRA...';try{const r=await Toolkit.speedTest(p=>{$('#toolResult').innerHTML=resultRows('Đang đo',[['Server',p.server||'--'],['Ping',p.ping!=null?p.ping.toFixed(0)+' ms':'--'],['Jitter',p.jitter!=null?p.jitter.toFixed(1)+' ms':'--'],['Download',p.download!=null?p.download.toFixed(2)+' Mbps':'--']]);});$('#toolResult').innerHTML=resultRows('Kết quả LibreSpeed',[['Server',r.server],['Ping',r.ping.toFixed(0)+' ms'],['Jitter',r.jitter!=null?r.jitter.toFixed(1)+' ms':'--'],['Download',r.download.toFixed(2)+' Mbps'],['Upload',r.upload.toFixed(2)+' Mbps']]);}catch(err){$('#toolResult').innerHTML=platformNote('Speed Test Web không thực hiện được: '+err.message+' Hãy dùng HLU TOOLS Android nếu cần kết quả native.');}finally{btn.disabled=false;btn.textContent='BẮT ĐẦU SPEED TEST';}return;}if(el.dataset.practice){await startAttempt(el.dataset.practice,'practice',10);return;}if(el.dataset.examConfig){examRuntime.configCustom=false;examRuntime.configCount=20;go('exam-config',{topic:el.dataset.examConfig});return;}if(el.dataset.examCount){examRuntime.configCount=Number(el.dataset.examCount);renderExamConfig();return;}if(el.dataset.startMock){const bank=examRuntime.bank,tp=bank.topics.find(x=>x.id===el.dataset.startMock),count=examRuntime.configCustom?examRuntime.configCount:tp.examQuestionCount;await startAttempt(tp.id,'test',count);return;}if(el.dataset.option){const at=examRuntime.attempt,q=at.questions[examRuntime.index],id=q.source.id;let s=new Set(examRuntime.selected[id]||[]);if(q.source.type==='true_false'){s=new Set([el.dataset.option]);if(at.mode==='practice')examRuntime.revealed[id]=true;}else{s.has(el.dataset.option)?s.delete(el.dataset.option):s.add(el.dataset.option);}examRuntime.selected[id]=[...s];renderExamRun();return;}if(el.hasAttribute('data-reveal')){const q=examRuntime.attempt.questions[examRuntime.index];examRuntime.revealed[q.source.id]=true;renderExamRun();return;}if(el.dataset.qIndex!=null){examRuntime.index=Number(el.dataset.qIndex);renderExamRun();return;}if(el.hasAttribute('data-mark')){const q=examRuntime.attempt.questions[examRuntime.index];examRuntime.marked[q.source.id]=!examRuntime.marked[q.source.id];renderExamRun();return;}if(el.hasAttribute('data-prev')){examRuntime.index=Math.max(0,examRuntime.index-1);renderExamRun();return;}if(el.hasAttribute('data-next')){if(examRuntime.index<examRuntime.attempt.questions.length-1)examRuntime.index++;else examRuntime.submitted=true;renderExamRun();return;}if(el.hasAttribute('data-submit-exam')){if(confirm('Nộp bài thi?')){examRuntime.submitted=true;renderExamRun();}return;}}
function openSection(sec){go('section',{section:sec});}
function handleInput(e){if(e.target.id==='searchInput'){const q=e.target.value;const rows=filterItems(q);const list=ROOT.querySelector('.content-list'),sum=ROOT.querySelector('.summary');sum.textContent=q?`${rows.length} kết quả cho “${q}”`:`Tất cả nội dung · ${rows.length} mục`;list.innerHTML=rows.length?rows.map(card).join(''):empty('⌕','Không tìm thấy kết quả','Thử một từ khóa khác.');history.replaceState({route},'',routeUrl('search',{q}));route.params.q=q;clearTimeout(handleInput.timer);handleInput.timer=setTimeout(()=>track(rows.length?'SEARCH':'SEARCH_NO_RESULT',{searchQuery:q,resultCount:rows.length}),500);}if(e.target.id==='sectionSearch'){const q=e.target.value;route.params.q=q;const rows=filterItems(q,route.params.section,route.params.brand||'');ROOT.querySelector('.summary').textContent=`${rows.length} mục • ${SECTION_SUB[route.params.section]}`;ROOT.querySelector('.content-list').innerHTML=rows.length?rows.map(card).join(''):empty('▣','Chưa có nội dung','');history.replaceState({route},'',routeUrl('section',route.params));}if(e.target.id==='customExam'){examRuntime.configCustom=e.target.checked;renderExamConfig();}}
function headerLeft(){const h=headerConfig();if(h.left==='menu')openDrawer();else back();}function headerRight(){const h=headerConfig();if(h.right==='bell')go('notifications');else if(h.right==='fav'&&currentItem)toggleFav(currentItem.id);else if(h.right==='external'&&currentItem){const u=previewUrl(currentItem);if(u)window.open(u,'_blank','noopener');}}
function bind(){document.addEventListener('click',handleClick);document.addEventListener('input',handleInput);document.addEventListener('change',handleInput);$('#headerLeft').addEventListener('click',headerLeft);$('#headerRight').addEventListener('click',headerRight);$('#scrim').addEventListener('click',closeDrawer);$('#feedbackForm')?.addEventListener('submit',feedbackSubmit);document.addEventListener('submit',e=>{if(e.target.id==='feedbackForm')feedbackSubmit(e);});window.addEventListener('popstate',()=>{route=parseRoute();render();});window.addEventListener('online',()=>{updateChrome();loadData(true,true);});window.addEventListener('offline',updateChrome);document.addEventListener('visibilitychange',()=>{if(document.visibilityState==='visible'&&navigator.onLine)loadData(false,true);});}
async function serviceWorker(){if('serviceWorker'in navigator)try{const r=await navigator.serviceWorker.register(BASE+'sw.js',{scope:BASE});r.update();}catch(_){}}
function start(){migrate();const cache=read(KEYS.data,[]);if(Array.isArray(cache)&&cache.length)resources=sortRows(cache.map(normalize));route=parseRoute();const ct=C.CONTACT||{};$('#developerLink').href=ct.zalo||'#';bind();render();track('SESSION_START');track('APP_OPEN');loadData(false,true);setInterval(()=>{if(navigator.onLine)loadData(false,true);},60000);window.addEventListener('load',serviceWorker,{once:true});}
start();
})();
